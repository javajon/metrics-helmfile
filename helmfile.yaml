repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: prometheus
    chart: bitnami/kube-prometheus
    version: 6.6.3
    # atomic: true
    # disableValidation: true
    values: 
    - charts/prometheus/values.development.yaml

  - name: grafana
    chart: bitnami/grafana
    version: 7.6.4
    values: 
    - charts/grafana-values.development.yaml

  - name: grafana-settings
    chart: charts/grafana-settings
    version: 0.1.0
